{
  "name": "renmito",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -n NODE,ANGULAR,IC -c blue,green,yellow \"cd backend && npm run dev\" \"cd frontend && npm start\" \"cd intelligence && uvicorn app.main:app --reload --port 8000\"",
    "build": "cd frontend && npm install && npx ng build --configuration production"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.4.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
